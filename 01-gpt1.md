# Improving Language Understanding by Generative Pre-Training

Alec Radford, Karthik Narasimhan, Tim Salimans, Ilya Sutskever (OpenAI, 2018)

GPT-2/3/4가 아니라 원조 GPT(GPT-1) 논문.

---

## 어떤 문제를 해결하려 했는가? (Abstract)

NLU 과제는 함의 판단, QA, 의미 유사도, 문서 분류처럼 종류가 다양한데, 과제마다 라벨은 적다. 그래서 과제 전용 모델은 데이터가 부족하면 잘 안 된다.

이 논문은 unlabeled 텍스트로 언어모델을 먼저 학습하고(generative pre-training), 그다음 각 과제에 discriminative fine-tuning을 하면 된다고 한다. 미세조정 때는 모델 구조를 거의 안 바꾸고, 입력을 과제에 맞게 시퀀스로 바꿔서 넣는다.

12개 과제 중 9개에서 당시 SOTA. Stories Cloze +8.9%, RACE +5.7%, MultiNLI +1.5%.

---

## 연구 동기와 문제점은 무엇인가? (Introduction)

딥러닝은 라벨이 많이 필요하다. 라벨이 적은 도메인에서는 unlabeled 텍스트를 쓰는 수밖에 없다. 당시엔 word2vec, GloVe 같은 단어 임베딩이 제일 잘 먹혔는데, 저자들은 단어보다 더 윗단계 의미를 옮기고 싶어 했다.

막힌 지점이 두 개다.

1. 어떤 목적함수가 전이 가능한 표현을 만드는지 의견이 갈린다. LM, 번역, discourse coherence 등이 과제마다 제각각 이겼다.
2. 배운 표현을 타깃 과제에 어떻게 붙일지도 제각각이다. 과제마다 아키텍처를 새로 달거나 학습 스케줄을 복잡하게 짜는 식이었다.

그래서 한 모델을 두 단계로 학습한다. 먼저 큰 원문 코퍼스에서 language modeling으로 초기화하고, 그다음 라벨 데이터로 미세조정한다. 타깃 과제가 사전학습 코퍼스와 같은 도메인일 필요는 없다.

모델은 Transformer를 쓴다. RNN보다 긴 의존성을 다루기 쉽다고 봐서다.

---

## 관련 연구 동향은 어떠한가? (Related Works)

Semi-supervised NLP는 원래 unlabeled 통계를 자질로 넣다가, word embedding으로 넘어갔다. sentence embedding도 있었지만 이 논문은 문서 길이의 생성 모델을 쓴다.

Unsupervised pre-training은 좋은 초기값을 찾는 방법이다. 제일 가까운 선행은 Dai & Le, ULMFiT인데 둘 다 LSTM이라 문맥이 짧다. ELMo나 CoVe는 사전학습 hidden을 자질로 붙인다. 과제마다 파라미터가 많이 생긴다. GPT-1은 네트워크를 거의 그대로 fine-tune한다.

보조 목적함수(Collobert & Weston, Rei)도 있다. 이 논문도 fine-tune 때 LM loss를 섞지만, 사전학습만으로도 언어 지식이 꽤 들어온다고 본다.

---

## 연구 접근법과 모델 구조는 어떻게 되는가? (Method)

학습은 두 단계다.

**1) Unsupervised pre-training**

코퍼스 \(U=\{u_1,\ldots,u_n\}\)에서 다음 토큰 확률을 최대화한다.

\[
L_1(U)=\sum_i \log P(u_i \mid u_{i-k},\ldots,u_{i-1};\Theta)
\]

\(k\)는 컨텍스트 길이. 모델은 Transformer decoder다. attention에 causal mask를 걸어서 앞쪽만 본다.

\[
h_0 = U W_e + W_p
\]
\[
h_l = \text{transformer\_block}(h_{l-1})
\]
\[
P(u)=\mathrm{softmax}(h_n W_e^\top)
\]

\(W_e\)는 토큰 임베딩이고 출력층과 공유한다. \(W_p\)는 학습되는 position embedding이다. 원 Transformer의 sinusoidal은 안 쓴다.

**2) Supervised fine-tuning**

입력 \(x_1,\ldots,x_m\)을 통과시켜 마지막 토큰 hidden \(h_l^m\)을 얻고, 선형층 \(W_y\)로 라벨 \(y\)를 예측한다.

\[
P(y \mid x)=\mathrm{softmax}(h_l^m W_y)
\]
\[
L_2(C)=\sum \log P(y \mid x)
\]

미세조정 때도 LM을 보조로 넣으면 일반화가 좋아지고 수렴이 빨라졌다. \(\lambda=0.5\).

\[
L_3(C)=L_2(C)+\lambda L_1(C)
\]

새로 생기는 파라미터는 \(W_y\)랑 delimiter 임베딩 정도다.

**입력 변환**

과제마다 머리를 새로 안 달고, 구조화된 입력을 한 시퀀스로 이어 붙인다. 앞에 `<s>`, 뒤에 `<e>`를 붙인다.

- entailment: premise + `$` + hypothesis
- similarity: 두 문장 순서를 둘 다 넣어서 각각 돌린 뒤 hidden을 더함
- QA: 문서 + 질문 + `$` + 후보답. 후보마다 한 번씩 넣고 softmax

**스펙**

12 layer, hidden 768, head 12, FFN 3072, 컨텍스트 512, BPE 4만. GELU, dropout 0.1, Adam. 사전학습 데이터는 BooksCorpus. 문장 단위로 섞인 1B Word Benchmark는 긴 구조가 깨져서 안 썼다. 토큰 PPL 18.4. fine-tune은 대체로 3 epoch.

---

## 실험 구성과 결과는? (Experiments)

NLI(SNLI, MNLI, QNLI, RTE, SciTail), QA(RACE, Story Cloze), 유사도(MRPC, QQP, STS-B), 분류(SST-2, CoLA), GLUE.

NLI는 RTE만 졌다. RTE는 학습셋이 2490개로 작다. 나머지는 이김. MNLI-m 82.1, SNLI 89.9, SciTail 88.3, QNLI 88.1.

QA는 Story Cloze 86.5(+8.9), RACE 59.0(+5.7).

CoLA는 35.0 → 45.4로 많이 올랐다. 사전학습 LM이 문법 분포를 이미 갖고 있어서 그런 것 같다. GLUE는 68.9 → 72.8. 12개 중 9개 SOTA.

---

## 무엇을 발견했고 한계점은 무엇인가? (Discussion)

레이어를 많이 옮길수록 MultiNLI, RACE가 오른다. 임베딩만 옮겨도 이득이고 full transfer가 제일 좋다.

사전학습만 한 LM으로도 대략적인 과제를 할 수 있다. CoLA는 토큰 log-prob, SST-2는 positive/negative 중 확률 높은 쪽, RACE는 답 문장의 평균 log-prob. 사전학습이 진행될수록 이 점수가 오른다. LSTM은 분산이 더 컸다.

Ablation:

- 사전학습 빼면 평균 74.7 → 59.9
- Transformer 대신 LSTM이면 69.1
- 보조 LM을 빼면 평균은 비슷한데, 데이터 큰 과제(NLI, QQP)에서는 넣는 편이 나았다

한계: RTE처럼 데이터가 너무 작으면 약하다. 단방향이라 양방향 문맥은 없다. 입력 포맷은 사람이 과제별로 설계해야 한다. BooksCorpus가 소설 위주라 도메인 편향도 있다.

---

## 결론 및 주요 요약은? (Conclusion)

하나의 Transformer를 생성 사전학습한 뒤 이해 과제에 fine-tune하는 프레임워크를 제안했다. 긴 텍스트로 장거리 의존성과 세계 지식을 배우고, 그걸 QA·유사도·함의·분류로 옮겼다. 비지도 학습으로 discriminative 과제를 올리는 게 실제로 되고, 그 조합으로 Transformer + 긴 텍스트가 잘 맞는다고 정리한다.

---

## 기존 연구 대비 차별점은 무엇인가?

word embedding은 단어 벡터만 옮긴다. ELMo는 BiLSTM LM을 자질로 붙이고 과제별 모델이 크다. ULMFiT는 LSTM이고 분류 중심이다.

GPT-1은 같은 decoder Transformer를 통째로 fine-tune하고, 과제 차이는 입력 포맷으로만 처리한다. 추가 파라미터는 거의 없다.

---

## 핵심 아이디어는?

다음 단어를 맞추려면 문법, 의미, 상식이 필요하니까 language modeling이 사전학습으로 쓸 만하다. 그걸 decoder-only Transformer에 넣고, fine-tune은 작게 한다. 미세조정 중에도 LM loss를 남겨서 생성 능력을 유지한다.

이후 GPT-2 zero-shot, GPT-3 in-context learning으로 이어지는 시작점이다. 몇 달 뒤 BERT는 양방향 MLM으로 이해 과제에서 더 앞선다.

---
